<p>
    ${language.index.obsolete}
    ${language.index.upgradePrefix}<a href="${language.index.upgradeUrl}">
    	${language.index.upgrade}
    </a>${language.index.upgradeSuffix}
</p>
<p>
	${language.index.chromePrefix}<a href="${language.index.chromeUrl}">
		${language.index.chrome}
	</a>${language.index.chromeSuffix}
</p>